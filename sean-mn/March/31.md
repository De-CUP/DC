# 2026.03.31 Tue

### 판타지 키우기 서버 .claude/ 폴더 수정

**TODO**
- `.claude/rules/OOO` 폴더 추가 및 파일 추가
- `.claude/hooks/preUseTool.sh` 및 `postUseTool.sh` 추가
- `AuthController.cs`의 `Login` 매서드 `CommonApiResponse` 래핑
- cicd 빌드 및 테스트 통합
- db 마이그레이션 (Account 테이블 추가 및 수정)

**Fix**
- 계정 생성 시 Role이 Admin으로 설정되는 보안 버그 수정


### DataGSM OpenAPI SDK for .NET

**TODO**
- `StudentDto`에 `Specialty`, `GithubId/GithubUrl` 추가 
- 버전 업데이트 및 BaseUrl 수정